spring:
  application:
    name: booking-monolith

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0
    validate-on-migrate: true
    clean-disabled: false

logging:
  name: booking-monolith-logger
  level:
    root: INFO

server:
  port: 8085
